// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAjzoFS8VTwsIaJaZa9drzveg9AWVlGnyc",
    authDomain: "canal-circle.firebaseapp.com",
    databaseURL: "https://canal-circle.firebaseio.com",
    projectId: "canal-circle",
    storageBucket: "canal-circle.appspot.com",
    messagingSenderId: "121149557393",
    appId: "1:121149557393:web:bd9e949db757914f"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
